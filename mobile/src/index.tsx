import React from 'react'
import { StatusBar } from 'react-native'

import AppProvider from './hook'

const src: React.FC = () => {
    return (
        <>
            <AppProvider>
                
            </AppProvider>
        </>
    )
}